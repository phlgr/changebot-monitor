# Environment variables referenced in .changebotrc.yml via ${VAR_NAME} syntax
# Copy this to .env and fill in your values

# ntfy.sh configuration (for ntfy channel type)
NTFY_TOPIC=example-topic # openssl rand -hex 8
NTFY_SERVER=https://ntfy.sh # optional, for self-hosted ntfy

# Webhook URLs (for webhook channel type)
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/...
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...
# CUSTOM_WEBHOOK_URL=https://your-api.com/webhook

# Authentication tokens for webhooks
# AUTH_TOKEN=your-bearer-token
